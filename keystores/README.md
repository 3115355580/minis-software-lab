# Image2Studio fixed debug signing key

This directory is intentionally local-only and ignored by Git.

`Image2Studio-debug.keystore` is used to keep sideload update signatures stable for future Image2Studio debug builds on this Minis device.

Do not delete it unless you are intentionally rotating the debug signing certificate. It is not committed to GitHub.
